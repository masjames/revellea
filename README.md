# Revellea HTML Prototype

Single-file static HTML prototype for Vercel deployment.

## Deploy

Deploy this directory as the Vercel project root:

```sh
vercel --prod
```

The prototype is self-contained in `index.html`; the logo is embedded as a data URI.
